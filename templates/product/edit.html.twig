{% extends 'base.html.twig' %}

{% block title %}Edit Product{% endblock %}

{% block body %}
    <div class="container">
        <div class="row">
            <div class="col-md-12">
                <h1>Edition du produit "{{ product.name }}"</h1>

                {{ include('product/_form.html.twig', {'button_label': 'Mettre à jour'}) }}

                <a href="{{ path('product_index') }}" class="btn btn-secondary">Retour à la liste</a>

            </div>
        </div>
    </div>
{% endblock %}
